name: venues_per_location
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  flutter_bloc: ^9.0.0
  provider: ^6.1.2
  equatable: ^2.0.7
  get_it: ^8.0.3
  shared_preferences: ^2.5.2
  rxdart: ^0.28.0
  mockito: ^5.4.4
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  network_image_mock: ^2.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/json/
